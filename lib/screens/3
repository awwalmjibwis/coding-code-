import 'package:flutter/material.dart';
import '../widgets/dashboard_card.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Jibwis AI"),
        centerTitle: true,
      ),
      body: ListView(
        padding: const EdgeInsets.all(16),
        children: const [
          DashboardCard(
            icon: Icons.show_chart,
            title: "Live Market",
            subtitle: "Track Forex & Crypto markets in real time.",
            color: Colors.green,
          ),
          SizedBox(height: 12),
          DashboardCard(
            icon: Icons.campaign,
            title: "Premium Signals",
            subtitle: "Receive high-quality trading signals.",
            color: Colors.orange,
          ),
          SizedBox(height: 12),
          DashboardCard(
            icon: Icons.smart_toy,
            title: "AI Analysis",
            subtitle: "Ask AI to analyze any trading chart.",
            color: Colors.blue,
          ),
          SizedBox(height: 12),
          DashboardCard(
            icon: Icons.school,
            title: "AI Academy",
            subtitle: "Learn trading step by step.",
            color: Colors.purple,
          ),
        ],
      ),
    );
  }
}